import Image from "next/image";
import { ShieldCheck } from "lucide-react";
import { ButtonLink } from "@/components/ui/Button";

/**
 * Dagsverkstedets signatur: full-bleed foto med tekstinnholdet i et HVITT KORT
 * oppå — ikke tekst direkte på bildet, og ingen scrim.
 *
 * Desktop: kortet er venstrejustert, maks 600px, med 28–56px luft over/under.
 * Mobil:   bildet er 290px høyt, kortet trekkes 44px OPP over bildekanten
 *          (-mt-11) og får 16px sidemarg.
 */
export function Hero() {
  return (
    <header className="relative border-b border-line hz:min-h-[min(620px,74vh)] hz:flex hz:items-center">
      <div className="relative h-[290px] overflow-hidden hz:absolute hz:inset-0 hz:h-auto">
        <Image
          src="/tjenester/komplett-bilpleie.webp"
          alt="Skum som skylles av svart lakk hos Handz On"
          fill
          priority
          sizes="100vw"
          className="object-cover"
        />
      </div>

      <div
        className="relative -mt-11 mx-4 mb-2 rounded-card-lg bg-surface p-[26px] pb-7 shadow-hero
                   hz:mt-0 hz:mb-0 hz:mx-0 hz:my-[clamp(28px,4vw,56px)] hz:ml-[clamp(20px,4vw,64px)]
                   hz:max-w-[600px] hz:p-[clamp(30px,3.4vw,50px)]"
      >
        <span className="block mb-6 font-heading text-[12px] font-semibold uppercase tracking-[.2em] text-navy">
          14 avdelinger · book mens du handler
        </span>

        <h1 className="font-heading text-[clamp(38px,5.4vw,64px)] font-bold leading-[.99] tracking-[-.032em] text-ink">
          Lever nøkkelen.
          <br />
          Hent bilen <span className="text-navy">ren.</span>
        </h1>

        <p className="mt-5 max-w-[42ch] text-[19px] leading-[1.55] text-body">
          Vi tar bilen mens du gjør ærendene dine på senteret. Håndvask med to bøtter,
          kontroll i lys før levering — og SMS når den står klar.
        </p>

        <div className="mt-7 flex flex-wrap gap-3">
          <ButtonLink href="/booking" size="lg" className="max-hz:flex-1">
            Bestill time
          </ButtonLink>
          <ButtonLink href="/avdelinger" variant="secondary" size="lg" className="max-hz:flex-1">
            Finn din avdeling
          </ButtonLink>
        </div>

        <p className="mt-[26px] flex items-center gap-2.5 border-t border-line pt-[18px] text-[14px] text-body-soft">
          <ShieldCheck aria-hidden className="size-[18px] shrink-0 text-status-open" strokeWidth={1.75} />
          Registrert i Arbeidstilsynets godkjenningsordning for bilpleie
        </p>
      </div>
    </header>
  );
}
