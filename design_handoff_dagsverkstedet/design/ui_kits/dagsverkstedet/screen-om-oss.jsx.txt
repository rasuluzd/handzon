/* Om oss — historie, HANDZON-verdier, tidslinje, bærekraft. */
(function () {
const D = window.HZ_DATA;
const { DGBtn: Btn, DGIcon: I, DGTrust: Trust } = window;

const VALUES = [
  ["H", "Handlekraft", "Vi tar bilen inn med en gang — ikke om tre uker."],
  ["A", "Ansvarlig", "Godkjent gjennom Arbeidstilsynets ordning. Ryddige lønns- og arbeidsvilkår."],
  ["N", "Nyskapende", "Renseanlegg som gjenbruker vann, og produkter vi tester før de rører lakken din."],
  ["D", "Direkte", "Faste priser og ærlig råd. Trenger bilen ikke polering, sier vi det."],
  ["Z", "Zen", "Rolig håndverk. Ingen hastverk, ingen roterende børster."],
  ["O", "Oppmerksom", "Vi ser dørkarmen, terskelen og baksiden av felgen."],
  ["N", "Nøye", "To bøtter, ren klut per panel, kontroll i lys før levering."],
];

const TIMELINE = [
  ["2005", "Ove Hagen åpner den første avdelingen — bilpleie inne på kjøpesenteret."],
  ["2014", "Franchisemodellen settes: hver avdeling er en egen lokal bedrift."],
  ["2021", "Renseanlegget settes i drift og gjenbruker vaskevannet."],
  ["2025", "20 år. 120 000 biler. 14 avdelinger fra Kristiansand til Ålesund."],
];

function OmOss({ go }) {
  return (
    <div>
      <div className="dg-banner"><img src={D.PHOTOS + "detaljering.webp"} alt="Detaljering av lakk hos Handz On" /></div>

      <div className="dg-pagehead">
        <div className="dg-eyebrow">Om oss</div>
        <h1 className="dg-h1" style={{ fontSize: "clamp(32px,4.2vw,52px)", marginTop: 14 }}>Kvalitet du kan stole på</h1>
        <p className="dg-lead" style={{ maxWidth: "62ch" }}>
          Handz On startet med én idé: at bilpleie skal passe inn i dagen din, ikke stjele den. I dag er vi Norges største kjede innen bilpleie, med 14 lokale avdelinger drevet av folk som bor i nabolaget. Visjonen er å samle alt innen bilpleie under ett tak — på Europas største kjøpesentre og lufthavner.
        </p>
      </div>

      <section className="dg-sec" style={{ paddingTop: 34 }}>
        <div className="dg-strip" style={{ borderTop: "1px solid var(--line)", margin: "0 calc(var(--pad) * -1)" }}>
          <div><b>14</b><span>avdelinger</span></div>
          <div><b>120 000+</b><span>biler behandlet</span></div>
          <div><b>20 år</b><span>siden 2005</span></div>
          <div><b>4,8</b><span>av 5 i score</span></div>
        </div>
      </section>

      <section className="dg-sec dg-sec--tight">
        <div className="dg-sechead" style={{ marginBottom: 20 }}>
          <div>
            <div className="dg-eyebrow">Verdier</div>
            <h2 className="dg-h2" style={{ marginTop: 12 }}>HANDZON</h2>
            <p className="dg-lead" style={{ marginTop: 10, fontSize: 17 }}>Sju bokstaver, sju krav vi stiller til hver eneste bil som kommer inn.</p>
          </div>
        </div>
        <div className="dg-values">
          {VALUES.map(([letter, title, text], i) => (
            <div key={title + i}>
              <b>{letter}</b>
              <div><div className="t">{title}</div><p>{text}</p></div>
            </div>
          ))}
        </div>
      </section>

      <section className="dg-sec dg-sec--alt">
        <div className="dg-eyebrow" style={{ marginBottom: 12 }}>Historie</div>
        <h2 className="dg-h2" style={{ marginBottom: 26 }}>Fra én avdeling til fjorten</h2>
        <div className="dg-timeline">
          {TIMELINE.map(([year, text]) => (
            <div key={year}><b>{year}</b><p>{text}</p></div>
          ))}
        </div>
      </section>

      <section className="dg-sec">
        <div className="dg-two" style={{ alignItems: "center" }}>
          <div>
            <div className="dg-eyebrow" style={{ marginBottom: 12 }}>Bærekraft</div>
            <h2 className="dg-h2" style={{ marginBottom: 20 }}>Mindre vann, mindre plast</h2>
            <ul className="dg-checks" style={{ gridTemplateColumns: "1fr" }}>
              {["Renseanlegg som gjenbruker vaskevannet — vi bruker en brøkdel av en hjemmevask med hageslange.",
                "Miljømerkede produkter, doserte i stedet for øst på.",
                "Egne dekkposer i resirkulert plast, og færre engangsprodukter for hvert år."].map((t) => (
                <li key={t}><I n="check" />{t}</li>
              ))}
            </ul>
          </div>
          <img src={D.PHOTOS + "utvendig-vask-og-voks.webp"} alt="Håndtørking av svart lakk"
               style={{ width: "100%", height: 300, objectFit: "cover", borderRadius: "var(--r-card-lg)" }} />
        </div>
      </section>

      <Trust />

      <section className="dg-sec dg-sec--tight">
        <div className="dg-acts" style={{ marginTop: 0 }}>
          <Btn size="lg" onClick={() => go("/booking")}>Bestill time</Btn>
          <Btn variant="s" size="lg" onClick={() => go("/avdelinger")}>Se alle avdelinger</Btn>
        </div>
      </section>
    </div>
  );
}

window.DGOmOss = OmOss;
})();
